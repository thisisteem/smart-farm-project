{
  "name": "smart-farm-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon -x node ./server.js || touch ./server.js",
    "start-test": "nodemon -x 'node ./app/bin/www || touch ./app/bin/www'",
    "dev": "NODE_ENV=development nodemon -x 'node ./server.js || touch ./server.js'",
    "prod": "NODE_ENV=production nodemon -x 'node ./server.js || touch ./server.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "http": "0.0.1-security",
    "morgan": "^1.10.0",
    "mysql2": "^2.3.3",
    "nodemon": "^2.0.15"
  }
}
